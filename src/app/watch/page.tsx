import WatchClient from "./watch-client";

export const metadata = {
    title: "LIVE STREAM | ParcFermé",
    description: "ParcFermé live integrated broadcast.",
};

export default function WatchPage() {
    return <WatchClient />;
}
