import WatchClient from "./watch-client";

export const metadata = {
    title: "LIVE STREAM | GridBox F1",
    description: "GridBox F1 live integrated broadcast.",
};

export default function WatchPage() {
    return <WatchClient />;
}
